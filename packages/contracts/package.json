{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vv",
    "deploy": "forge script script/Deploy.s.sol --rpc-url $MANTLE_SEPOLIA_RPC --broadcast --verify",
    "clean": "forge clean"
  }
}
